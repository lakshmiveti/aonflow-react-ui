import React from "react";

function Settings() {
  return (
    <div className="logout-and-setting">
      <ul>
        <li>Settings</li>
        <li>Logout</li>
      </ul>
    </div>
  )
}

export default React.memo(Settings);
